name: My workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
          - name: welcome message
            run: echo "My first Github Actions Job"
             
          - name: List files
            run: ls
            working-directory: action-1